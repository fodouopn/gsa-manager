# Variables d'environnement pour production
# Copier ce fichier vers .env et remplir les valeurs
# IMPORTANT: Ne jamais commiter le fichier .env !

# ============================================
# Django Configuration
# ============================================
# Générer avec: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=change-me-in-production-generate-with-django-utils

# JWT Secret Key (peut être différent de SECRET_KEY)
JWT_SECRET_KEY=change-me-in-production-jwt-secret-key

# Debug mode (False en production)
DEBUG=False

# Allowed hosts (séparés par des virgules, sans espaces)
# Exemple: ALLOWED_HOSTS=votre-domaine.com,www.votre-domaine.com
ALLOWED_HOSTS=votre-domaine.com

# CORS Allowed Origins (séparés par des virgules, sans espaces)
# Exemple: CORS_ALLOWED_ORIGINS=https://votre-domaine.com,https://www.votre-domaine.com
CORS_ALLOWED_ORIGINS=https://votre-domaine.com

# ============================================
# Database Configuration
# ============================================
POSTGRES_DB=gsa_db
POSTGRES_USER=gsa_user
POSTGRES_PASSWORD=change-me-strong-password-here
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# ============================================
# Redis Configuration
# ============================================
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0

# ============================================
# Super Admin Account
# ============================================
# Compte super admin créé automatiquement au premier démarrage
SUPER_ADMIN_EMAIL=admin@gsa.fr
SUPER_ADMIN_PASSWORD=change-me-strong-password-here

# ============================================
# Domain Configuration
# ============================================
# Domaine principal (utilisé pour Caddy et frontend)
DOMAIN=votre-domaine.com

# URL API pour le frontend (utilisé dans Vite build)
# Exemple: VITE_API_URL=https://votre-domaine.com/api
VITE_API_URL=https://votre-domaine.com/api

# ============================================
# Email Configuration (optionnel)
# ============================================
# Si vous voulez envoyer des emails (relances factures, etc.)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=votre-email@gmail.com
# EMAIL_HOST_PASSWORD=votre-mot-de-passe-app
# DEFAULT_FROM_EMAIL=noreply@votre-domaine.com

# ============================================
# Backup Configuration (optionnel)
# ============================================
# Chemin pour les backups (sur le VPS)
# BACKUP_DIR=/var/backups/gsa
# BACKUP_RETENTION_DAYS=7
